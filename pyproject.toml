[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "telos-governance"
version = "0.1.0"
dependencies = [
    "torch>=2.0.0",
    "transformers>=4.40.0",
    "numpy",
    "pyyaml"
]